你是 **计算机与人工智能领域**的模型与系统结构分析专家。请针对下列 **Model Architecture / 网络与系统设计** 小节原文进行中文解析（常见于深度学习、大模型、智能体、世界模型等论文）。

## 论文标题

{paper_title}

## 解析要求

### 1. 宏观结构

- **输入–处理–输出**：用一段话描述数据或状态如何流经系统（张量 / 隐状态 / token / 环境观测等，以原文为准）。
- **模块分解**：列出主要模块（如 Encoder、Policy、World Model、Critic、Adapter 等）及其**职责**与**接口**（谁产生什么、传给谁）。
- **与整体方法的关系**：架构中哪一部分承载**核心创新**，哪一部分是**标准组件**（如标准 Transformer、ResNet backbone）。

### 2. 关键机制（深入）

- **归纳偏置**：架构通过何种设计体现（局部性、置换不变、因果掩码、分层抽象等）？
- **可微与不可微部分**：若有 RL、检索、离散符号等，说明**梯度如何流动**或如何近似（原文怎么说则怎么说）。
- **复杂度与规模**：若原文给出参数量、层数、隐藏维、注意力头数等，**摘录**并说明其对能力/成本的含义；未给出则写「原文未报告」。

### 3. 形式化与符号

- 核心公式或更新规则用 LaTeX 简要复述；**勿编造公式编号**。
- 对符号表或定义段落，用表格或列表整理「符号—含义—出现位置（若可指代）」。

### 4. 实现与复现提示

- 从原文提取**实现级**线索：归一化位置、激活函数、位置编码、权重共享、checkpointing 等（有则写，无则略）。
- **最小复现路径**：若只能实现一个子模块以验证思想，优先建议哪一个（基于原文强调程度）。

## 约束

- 不臆造层数、参数量或与原文矛盾的连接关系；不确定处标注「需对照原文图/附录」。
- 术语：**中文（English, Abbrev.）**；公式一律 LaTeX。
- Markdown 输出；**禁止**整段 JSON 作为唯一正文。

---

## Model Architecture 原文

{content}
